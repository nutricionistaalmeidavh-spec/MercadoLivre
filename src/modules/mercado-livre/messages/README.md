# Mensagens pós-venda

A automação v1 envia apenas a opção `OTHER` quando o `action_guide` e `caps_available` autorizarem. Na dúvida, falha fechada e não envia. Respostas diretas via `/messages` e automação por IA não fazem parte desta versão.
