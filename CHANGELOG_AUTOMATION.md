# Automation v1

Adiciona persistência server-side de tokens, SQLite, webhook `orders_v2`, fila persistente, worker com retry, normalização/elegibilidade de pedidos, action guide/caps, policy guard, mensageria `OTHER`, idempotência, estados de automação, health/admin API, runtime Docker e testes unitários.
